<Window x:Class="TLVanced.Ue4ConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="UE4 — Engine.ini"
        Height="640"
        Width="1010"
        MinHeight="480"
        MinWidth="770"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        Loaded="Window_Loaded">
    <Window.Resources>
        <Style x:Key="IniRowTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding HasValueInFile}" Value="True">
                    <Setter Property="Foreground" Value="#FFC5E1A5"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="IniValueEditStyle" TargetType="TextBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#FF252525"/>
            <Setter Property="BorderBrush" Value="#FF3A3A3A"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="Padding" Value="2"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding HasValueInFile}" Value="True">
                    <Setter Property="Foreground" Value="#FFC5E1A5"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Border BorderThickness="1" BorderBrush="#FF282828" Background="#FF141414" CornerRadius="3"
            MouseLeftButtonDown="MoveWindow">
        <Grid Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid>
                <TextBlock Text="UE4 — RendererSettings" VerticalAlignment="Center" FontSize="16"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="ReloadButton" Content="Обновить" Padding="12 4" Margin="0 0 8 0" Cursor="Hand" Click="ReloadButton_Click"/>
                    <Button x:Name="SaveButton" Content="Сохранить" Padding="12 4" Margin="0 0 8 0" Cursor="Hand" Click="SaveButton_Click"/>
                    <Button Width="32" Height="28" Padding="0" Cursor="Hand" Click="CloseWindow_Click" ToolTip="Закрыть">
                        <ui:SymbolIcon Symbol="Dismiss48" Foreground="White" FontSize="12"/>
                    </Button>
                </StackPanel>
            </Grid>

            <TextBlock x:Name="SectionHint" Grid.Row="1" TextWrapping="Wrap" Margin="0 0 0 8" Foreground="#FFFFB74D"
                       Visibility="Collapsed"/>

            <DataGrid x:Name="SettingsGrid" Grid.Row="2"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      HeadersVisibility="Column"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      GridLinesVisibility="Horizontal"
                      RowBackground="#FF1A1A1A"
                      AlternatingRowBackground="#FF141414"
                      Background="#FF141414"
                      Foreground="White"
                      BorderBrush="#FF282828"
                      HorizontalGridLinesBrush="#FF282828"
                      Margin="0 0 0 8">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#FF222222"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Padding" Value="8 6"/>
                        <Setter Property="BorderBrush" Value="#FF282828"/>
                        <Setter Property="BorderThickness" Value="0 0 1 1"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderBrush" Value="#FF282828"/>
                        <Setter Property="Padding" Value="6 4"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#FF2D2D2D"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Параметр" Binding="{Binding Key}" Width="2*" IsReadOnly="True"
                                        ElementStyle="{StaticResource IniRowTextStyle}"/>
                    <DataGridTextColumn Header="Значение" Binding="{Binding EditingValue, UpdateSourceTrigger=PropertyChanged}" Width="*"
                                        ElementStyle="{StaticResource IniRowTextStyle}"
                                        EditingElementStyle="{StaticResource IniValueEditStyle}"/>
                    <DataGridTextColumn Header="Допустимые значения" Binding="{Binding AllowedValues}" Width="2*" IsReadOnly="True"
                                        ElementStyle="{StaticResource IniRowTextStyle}"/>
                    <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="3*" IsReadOnly="True"
                                        ElementStyle="{StaticResource IniRowTextStyle}"/>
                </DataGrid.Columns>
            </DataGrid>

            <TextBlock x:Name="PathText" Grid.Row="3" TextWrapping="Wrap" FontSize="11" Foreground="#FF9E9E9E" Margin="0 0 0 4"/>

            <TextBlock x:Name="FileMissingHint" Grid.Row="4" TextWrapping="Wrap" Foreground="#FFFF5252"
                       Visibility="Collapsed"/>
        </Grid>
    </Border>
</Window>
